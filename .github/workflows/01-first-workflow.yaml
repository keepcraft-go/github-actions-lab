name: First Workflow

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Hello
        run: echo "hello github actions"

      - name: Check OS
        run: uname -a

      - name: Runner Information
        run: | 
          hostname
          whoami
          pwd 
          uname -a